# Example scripts

Noah Tuchow referenced a zip of example scripts in email; the attachment could not be extracted or opened on our side, so these three files were added here to match the roles he described:

| File | Purpose |
|------|---------|
| `example_gen_set_args.py` | Illustrates `Generator.set_args` before `generate()`. |
| `test_pre_generator.py` | Illustrates `Pre_Generator` plus continuing the pipeline with `d` and `idx_start`. |
| `debug_changes_for_DI.py` | Compares `compute_yield` methods for direct imaging (`detectable` vs `scaling_relation`). |

If the original zip becomes available, these can be diffed or replaced so the repo matches Noah’s files exactly.
